- Var olan backend ile React  uygulaması arasında bağlantı kuruldu, API istekleri için gerekli yapılandırmalar api.ts dosyası üzerinden tamamlanmıştır.

- Ürün listesi (/api/product) ve stok hareketleri (/api/stockmovement) endpoint’leri hem tarayıcı üzerinden hem de uygulama içinde test edilerek doğru çalıştığı doğrulandı.

- Uygulama genelinde kullanılacak ortak bileşenler (AppText, AppCard, AppButton vb.) geliştirildi.

- Oluşturulan bileşenlerin tasarım ve stil uyumunu test edebilmek amacıyla test-ui ekranı hazırlanıp incelenmiştir.